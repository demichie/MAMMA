To compile:

./configure
make
make install


The executable is copied in the bin folder
To test the code copy the executable in the EXAMPLES folder and:

./MAMMA

A Matlab script (plot_solution.m) is provided in the EXAMPLES folder to plot the results.


